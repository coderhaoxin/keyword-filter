{
  "name": "keyword-filter",
  "version": "0.5.0",
  "description": "keyword filter for node.js",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha -R spec -t 20 test/*.js",
    "test-cov": "istanbul cover node_modules/.bin/_mocha -- -R dot -t 20 test/*.js",
    "test-travis": "istanbul cover node_modules/.bin/_mocha --report lcovonly -- -R dot -t 20 test/*.js"
  },
  "repository": "coderhaoxin/keyword-filter",
  "keywords": [
    "keyword",
    "replace",
    "filter"
  ],
  "author": "haoxin",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "eslint-config-ok": "github:haoxins/eslint-config",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2"
  },
  "eslintConfig": {
    "extends": [
      "ok"
    ]
  }
}
